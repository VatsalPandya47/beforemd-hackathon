# Demo assets

Add these before the event (doc section 9, Vatsal's ticket "Record backup patient audio"):

- `avatar-maya.png` — synthetic patient avatar for the clinician dashboard card.
- `backup-call.wav` — recorded backup audio of the scripted conversation (doc section 2), used by replay mode when the live mic/Deepgram path fails.

Not committed by default since they're binary recordings the team creates during prep.
